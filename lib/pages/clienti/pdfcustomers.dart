import 'package:flutter/material.dart';

class PDFCustomers extends StatelessWidget {
  const PDFCustomers({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PDF Customers')),
      body: const Center(child: Text('PDF Customers Page')),
    );
  }
}
